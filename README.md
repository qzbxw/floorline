# Floorline

Торговый терминал по Telegram Gifts на Tonnel: один Go-бинарь, который следит за
фидом листингов и реальных сделок, полностью перепроверяет каждый кандидат и
присылает в Telegram готовую карточку с решением. Если лот проходит **все**
проверки и модель реально легко продать — покупает сам и сразу выставляет обратно.

```
go build -o floorline ./cmd/floorline
cp .env.example .env && chmod 600 .env    # заполни три обязательных поля
./floorline smoke                          # проверка, что API отвечает
./floorline backfill                       # 14 дней истории сделок
./floorline run                            # поллеры + бот
```

---

## В чём идея

Флор коллекции — шум. Торгуемая единица — пара **(коллекция, модель)**: Plush Pepe
с моделью 0.4% и с моделью 15% — это разные активы с разной ценой и разной
ликвидностью.

И главное — **триггер**. Все публичные боты считают «скидку к флору». Это неверно:
если ты покупаешь сам флор-лот, то после покупки флором становится **следующий
аск**, и продавать ты будешь относительно него.

```
P        цена лота
A1       лучший аск по модели, ИСКЛЮЧАЯ сам лот
med      медиана реальных сделок за 14 дней

exit     = min( A1 × (1 − undercut),  med )     ← берём худшее из двух
proceeds = exit × (1 − fee)
edge     = (proceeds − P) / P
```

Лот может быть «−25% к флору» и не давать ни копейки, если второй аск стоит
рядом. Floorline это видит, «скидка к флору» — нет.

У Тоннеля нет комиссии за продажу (только ~0.5% реферального сбора), поэтому
порог входа задаёт не стоимость сделки, а **ликвидность**: скидка на модель,
которая продаётся раз в месяц, — это не прибыль, а склад.

---

## Настройка

### 1. Telegram

Токен у [@BotFather](https://t.me/BotFather), свой numeric id — у
[@userinfobot](https://t.me/userinfobot). Всё, что приходит с других аккаунтов,
игнорируется молча: бот распоряжается деньгами, поэтому он однопользовательский.

### 2. Tonnel authData

Открой `market.tonnel.network` в десктопном браузере → DevTools → Application →
Local Storage → скопируй значение `authData`. Это query-строка, начинающаяся с
`query_id=` или `user=`.

Она протухает. Когда это случится, бот пришлёт уведомление — отправь ему
`/auth <новая строка>`, редактировать `.env` не нужно. Новое значение
проверяется сразу и сохраняется в БД; при отказе остаётся старое.

Без валидной сессии не работает **ничего**, включая чтение стакана: реверс-доки
утверждают, что `pageGifts` можно дёргать анонимно с пустым `user_auth`, но по
факту (проверено) он отвечает `Invalid auth data`.

### 3. Portals (необязательно)

Даёт строку кроссмаркет-сравнения в карточке. `portals-market.com` → DevTools →
Network → любой запрос к API → заголовок `Authorization`. Без него строка просто
не показывается. **Floorline там не торгует** — вывод гифта из оффчейн-движка
Тоннеля занимает минуты-часы, любой спред за это время умрёт. Это только проверка
на вменяемость: если два маркета сильно расходятся, одна из цифр врёт.

---

## Команды

```
Рынок
  /floor Plush Pepe                    модели коллекции, флор, саплай, редкость
  /floor Plush Pepe / Pink Diamond     одна модель + топ-5 асков
  /book  Plush Pepe / Pink Diamond     лестница асков и глубина
  /hist  Plush Pepe / Pink Diamond     реальные сделки, медиана, velocity
  /val 123456                          полная оценка лота + какие гейты не прошёл

Книга
  /pos            открытые позиции с текущей переоценкой
  /pnl            реализованный и нереализованный, нетто
  /balance
  /relist 123456  переставить свой лот по текущему стакану

Алерты
  /watch Plush Pepe / Pink Diamond 1200
  /unwatch, /watchlist
  /mute Plush Pepe 4        заглушить коллекцию на 4 часа
  /unmute Plush Pepe

Автопокупка
  /arm, /disarm
  /limits
  /limits set max_ticket 50

Служебное
  /status         поллеры, свежесть данных, прогрев, статистика за сутки
  /auth <данные>  заменить протухшую сессию
```

Разделитель `/` обязателен: и коллекция, и модель содержат пробелы, иначе
`/book Plush Pepe Pink Diamond` не разобрать. Имена можно писать частично и в
любом регистре — если совпадение однозначное, бот его найдёт.

---

## Как это работает внутри

Один процесс, пять поллеров, SQLite. Ни докера, ни отдельной СУБД.

| Поллер | Запрос | Период | Зачем |
|---|---|---|---|
| `feed` | `pageGifts` sort=latest | 2 с | новые листинги и подешевевшие релисты |
| `stats` | `filterStats` | 60 с | флор+саплай+редкость **по всему рынку одним запросом** |
| `sales` | `saleHistory` | 25 с | реальные сделки → velocity, медианы, sweep |
| `book` | `pageGifts` по модели | по требованию, кэш 15 с | лучший конкурирующий аск |
| `inventory` | `myGifts` + баланс | 60 с | позиции, undercut, залежавшийся товар |

`filterStats` — основа всего: один запрос возвращает флор и саплай каждой модели
каждой коллекции. Поэтому оценка почти ничего не стоит по трафику, а стакан
дёргается только для тех единиц, что реально попали в кандидаты.

**Дешёвый пре-фильтр.** Цена выхода не может превысить медиану сделок, поэтому
потолок достижимого edge считается локально, без единого запроса. Это отсекает
почти весь фид до того, как понадобится сеть.

### Прогрев

Velocity нельзя посчитать по базе, которая знает только последние пять минут.
При первом запуске бот качает 14 дней истории (`backfill`) и до её готовности
**не покупает ничего сам**, о чём честно пишет в `/status`.

### Cloudflare

`gifts2.tonnel.network` и `gifts.coffin.meme` отдают 403 обычному Go-клиенту из-за
TLS-фингерпринта. Используется `bogdanfinn/tls-client` с профилем Chrome и одним
стабильным User-Agent на процесс. Популярные Python-обёртки рандомят UA на каждый
запрос — это само по себе аномалия: один TLS-отпечаток с прыгающим UA не выдаёт
ни один реальный браузер.

Три отказа подряд → уведомление, поллеры уходят в бэкофф, автопокупка на паузе.

`floorline smoke` отличает эти два случая явно: блокировка (403/429) значит, что
сломан транспорт и не заработает ничего; отказ по сессии значит, что транспорт в
порядке и менять надо только authData.

### Подпись `wtf`

Каждый write-запрос требует поле `wtf` — AES-256-CBC от текущего timestamp по
схеме OpenSSL `Salted__` с MD5-KDF. Реализовано на stdlib и проверено тестом
против эталонных векторов OpenSSL: если он падает, покупка и листинг перестанут
работать целиком.

---

## Автопокупка

Три барьера подряд. Пройти надо все.

**1. Гейт сигнала** — то, что вообще заслуживает внимания:
edge ≥ 5% · velocity ≥ 1 сделка/день · ≥ 10 сделок за 14 дней · дисперсия ≤ 35% ·
тренд ≥ 0.90.

**2. Гейт автопокупки** — «это точно легко продать»:
edge ≥ 10% · velocity ≥ 2/день · ≥ 20 сделок · **≥ 4 разных продавца** (защита от
накрутки) · дисперсия ≤ 25% · тренд ≥ 0.95 · ожидаемое время выхода ≤ `max_exit_days` ·
прогрев завершён.

**3. Риск-лимиты** — сколько денег вообще можно потратить:

```
/limits set max_ticket 50
/limits set daily_budget 300
/arm
```

`max_ticket` и `daily_budget` **обязательны** — без них `/arm` откажет. Дневной
бюджет лежит в БД, поэтому рестарт не вернёт уже потраченное.

Прочие лимиты: `max_positions`, `max_buys_per_hour`, `model_cooldown_min`,
`min_balance_reserve`, `min_markup`, `max_exit_days`.

**Авто-разоружение** при: трёх неудачных покупках подряд · 403 от write-хоста ·
отклонённой сессии Tonnel.

**Идемпотентность.** Заявка на покупку резервируется в БД *до* отправки запроса,
поэтому двойной тап по кнопке, ретрай или падение посреди операции не превратятся
в две покупки. При обрыве связи бот не повторяет запрос, а спрашивает у маркета,
чем мы владеем.

**Автолистинг.** Сразу после покупки стакан перечитывается и лот выставляется по
пересчитанной цене выхода. Если рынок за это время ушёл и продажа означала бы
цену ниже `вход + min_markup` — бот **не выставляет** и пишет об этом. Фиксировать
убыток за тебя он не будет.

---

## Что ещё детектится

- **Undercut** — твой аск перестал быть лучшим.
- **Залежавшаяся позиция** — висит втрое дольше, чем velocity модели предсказывает.
- **Sweep** — всплеск сделок по модели от нескольких разных покупателей.
- **Обвал флора** — по вотчлисту и позициям, падение >15% за час.
- **Новая коллекция** — незнакомое имя в `filterStats`. Первые часы дропа —
  самое волатильное окно на этом рынке.

## Встроенный бэктест

Каждый сигнал пишется в таблицу `signals` вместе с ценой, выходом и edge. Сделки
копятся в `sales`. Через пару недель хватит одного SQL-джойна, чтобы посчитать,
какая доля сигналов реально продалась выше входа и за сколько дней — без единой
строчки дополнительного кода. **Не поднимай пороги и не арми автопокупку, пока не
посмотришь на эти цифры.**

---

## Разработка

```
make test     # go vet + go test ./...
make build
make smoke    # требует живой .env
```

Структура:

```
cmd/floorline      CLI: run | smoke | backfill
internal/tonnel    приватное API: клиент, подпись wtf, фильтры, типы
internal/store     SQLite: листинги, сделки, снапшоты, позиции, сигналы, риск
internal/pricing   ликвидность, стакан, оценка (здесь живёт триггер)
internal/signal    гейты сигнала и автопокупки, скоринг, дедуп
internal/risk      лимиты, kill switch, дневной бюджет
internal/exec      покупка + автолистинг + идемпотентность
internal/app       поллеры, детекторы, рендер карточек
internal/bot       Telegram
internal/portals   read-only сравнение цен
```

## Оговорки

Эндпоинты Tonnel приватные и недокументированные — они могут измениться без
предупреждения. Всё изолировано в `internal/tonnel`; `floorline smoke` за
несколько секунд скажет, что именно сломалось.

Автоматизация может противоречить правилам площадки. Дефолтные частоты поллинга
подобраны так, чтобы нагрузка выглядела человеческой; поднимать `READ_RPS` —
быстрейший способ словить бан.

`.env` и `*.db` в `.gitignore`. Токен и authData не пишутся в логи.
