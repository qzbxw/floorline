# Floorline

Торговый терминал по Telegram Gifts на Tonnel: один Go-бинарь, который следит за
фидом листингов и реальных сделок, полностью перепроверяет каждый кандидат и
присылает в Telegram готовую карточку с решением. Если лот проходит **все**
проверки и модель реально легко продать — покупает сам и сразу выставляет обратно.

```
go build -o floorline ./cmd/floorline
cp .env.example .env && chmod 600 .env    # заполни три обязательных поля
./floorline smoke                          # проверка, что API отвечает
./floorline gram                           # GRAM/USDT и лаг отслеживаемых флоров
./floorline history 10368454               # полный lifecycle конкретной позиции
./floorline val 10368454 10382082          # оценка лотов без Telegram: цена и гейты
./floorline login                          # разовый вход в Telegram: мини-аппы MRKT/Portals
./floorline backfill                       # 60 дней: ликвидность + премии атрибутов
./floorline run                            # поллеры + бот
```

---

## В чём идея

Флор коллекции — шум. Торгуемая единица — пара **(коллекция, модель)**: Plush Pepe
с моделью 0.4% и с моделью 15% — это разные активы с разной ценой и разной
ликвидностью.

И главное — **триггер**. Все публичные боты считают «скидку к флору». Это неверно:
если ты покупаешь сам флор-лот, то после покупки флором становится **следующий
аск**, и продавать ты будешь относительно него.

```
ask       цена лота
cost      ask × 1.005 — сколько реально спишется с покупателя
walkaway  самый дешёвый чужой оффер на любой площадке
fast_exit walkaway × (1 − undercut) — цена, по которой мы самые дешёвые
edge      = (fast_exit − cost) / cost
```

Лот может быть «−25% к флору» и не давать ни копейки, если второй аск стоит
рядом. Floorline это видит, «скидка к флору» — нет.

**Быстрый выход — это арифметика, а не блендинг.** Продать быстро можно только
став самым дешёвым предложением на экране, поэтому fast_exit равен андеркату
лучшего чужого оффера — на Tonnel или на другой площадке, что дешевле. Блендинг
живого стакана, истории, внешней глубины и трейтов отвечает на другой вопрос
(«сколько модель стоит») и живёт в `fair_value`.

Раньше эти два вопроса решались одной формулой: fast_exit собирался из медианы
первых трёх асков, а walkaway применялся снизу как пол. Медиана трёх асков по
построению стоит **за очередью**, так что бот регулярно обещал цену, по которой
никто не купит. По логам за 12–13 августа эдж был завышен в среднем ровно вдвое,
а в двух случаях из восьми у него был неверный знак. Jolly Chimp с заявленным
выходом 7.326 при чужом аске 7.00 продался за 6.98 — ровно андеркат этого 7.00,
который движок уже посчитал и печатал строкой выше как «слить сейчас».

Регрессионный набор из этих восьми карточек лежит в
`internal/pricing/golden_test.go`.

У Тоннеля нет комиссии за продажу; ~0.5% referral добавляется покупателю поверх
аска (например, 3.79 списывается как 3.809). Поэтому
порог входа задаёт не стоимость сделки, а **ликвидность**: скидка на модель,
которая продаётся раз в месяц, — это не прибыль, а склад.

---

## Настройка

### 1. Telegram

Токен у [@BotFather](https://t.me/BotFather), свой numeric id — у
[@userinfobot](https://t.me/userinfobot). Всё, что приходит с других аккаунтов,
игнорируется молча: бот распоряжается деньгами, поэтому он однопользовательский.

### 2. Tonnel authData

⚠️ **В Local Storage её нет** — вопреки всем публичным гайдам. Текущий фронт
(`marketplace.tonnel.network`) держит `initData` в памяти и подставляет в каждый
запрос. В Local Storage лежит только `profile-data`, `server-ru` и тема.

Два способа достать:

**Через консоль** (быстрее). Открой мини-апп Тоннеля с DevTools, в Console
переключи селектор фрейма с `top` на `marketplace.tonnel.network` и выполни:

```js
copy(Telegram.WebApp.initData)
```

**Через Network** (надёжнее, работает независимо от фреймов). Вкладка Network →
фильтр `gifts` → любой POST на `gifts2.tonnel.network` → Payload → скопируй
значение поля `user_auth` (в других эндпоинтах оно называется `authData`).

Строка выглядит так: `query_id=...&user=%7B%22id%22...&auth_date=...&hash=...`

Она протухает. Когда это случится, бот пришлёт уведомление — отправь ему
`/auth <новая строка>`, редактировать `.env` не нужно. Новое значение
проверяется сразу и сохраняется в БД; при отказе остаётся старое.

Без валидной сессии не работает **ничего**, включая чтение стакана: реверс-доки
утверждают, что `pageGifts` можно дёргать анонимно с пустым `user_auth`, но по
факту (проверено) он отвечает `Invalid auth data`.

### 3. Portals и MRKT (кроссмаркет-сравнение)

Дают строки сравнения цен в карточке и в `/val`. Площадки независимы.

**Portals — работает сразу, настраивать нечего.** Его read-эндпоинты отвечают
без авторизации. `PORTALS_AUTH_DATA` в конфиге есть только на случай, если это
изменится.

⚠️ Домен переехал: `portals-market.com` (на который ссылаются все публичные
Python-обёртки) больше не резолвится. Живой хост — **`portals.tg`**, и формат
ответа у него тоже другой: `collections[slug].models[]` вместо старой карты
`floor_prices`. Проверено запросом — реализовано под новый формат.

**MRKT — нужны креды.** Лучше `MRKT_INIT_DATA`: открой мини-апп MRKT в Telegram
Web → DevTools → Network → POST на `api.tgmrkt.io/api/v1/auth` → поле `data` из
тела запроса. Клиент сам обменяет её на токен и **обновит, когда тот протухнет**.
Либо готовый токен в `MRKT_TOKEN` — проще достать, но сам не продлится.

**Floorline там не торгует, но ценой управляет именно оттуда.** Вывод гифта из
оффчейн-движка Тоннеля занимает минуты-часы плюс газ, так что спред между
площадками неисполним. Зато наш покупатель ходит куда хочет: если на Portals та
же модель стоит дешевле, чем мы собрались продавать, продадим мы не по своей
цене. Поэтому самый дешёвый чужой оффер — это потолок выхода, а не сноска.

Сравнимость ищется по лестнице **точные трейты → фон → модель**, и ступень
записывается в карточку. Раньше при отсутствии точного совпадения бот сразу
падал на весь модельный стакан — то есть оценивал Onyx Black по обычным
экземплярам. Это разные активы.

**Но одной ступени мало.** Показывалась только та, на которой нашлось
совпадение, а остальные выбрасывались — и это врало в сторону, которая стоит
денег. Spring Basket · Midas Bunny на Old Gold: карточка писала «Portals
19 / 20 / 24 · по фону · +42% к входу», что читается как поддержка входа 14.07.
На той же площадке Midas Bunny без разбора фона начинались с **7.48** — вдвое
дешевле. Покупателю, которому нужна модель, а не конкретный фон, ничто не мешает
взять тот.

Теперь модельный стакан едет рядом с узкой ступенью: узкая по-прежнему ограничивает
выход (это специально), а модельная показывается и поднимает предупреждение, когда
она ниже нашего входа. Фильтровать усечённый прайс-сортированный список нельзя —
среди двадцати самых дешёвых Midas Bunny нет ни одного Old Gold, — поэтому это
отдельный запрос, и он делается только на карточке и в позициях. Скан спрашивает
площадки без атрибутов, у него ступень и так модельная, так что ему это не стоит
ничего.

Обе площадки опрашиваются **параллельно** с дедлайном 4 секунды: медленная не
задержит карточку, а её ответ просто не покажется. Флоры кэшируются на 5 минут
(`CROSS_MARKET_TTL`), включая отказы — упавшая площадка не дёргается на каждый
сигнал.

---

## Команды

```
Рынок
  /floor Plush Pepe                    модели коллекции, флор, саплай, редкость
  /floor Plush Pepe / Pink Diamond     одна модель + топ-5 асков
  /book  Plush Pepe / Pink Diamond     лестница асков и глубина
  /hist  Plush Pepe / Pink Diamond     реальные сделки, медиана, velocity
  /val 123456                          полная оценка лота + какие гейты не прошёл
  /scan                                скан стоящих лотов под свободный баланс
  /scan 3-5                            свой диапазон цен вместо баланса
  /scan -5                             только потолок; /scan 3- только пол
  /scan 25                             показать до 25 (и шире пройтись)
  /scan Plush Pepe 3-5 25              коллекция + диапазон + лимит, в любом порядке
  /trade                               сессия: отобрать ликвидные пары и торговать по ним
  /trade off                           выйти из сессии
  <ссылка из Tonnel>                   то же самое: кинь боту Share-ссылку на гифт

Книга
  /pos            открытые позиции с текущей переоценкой
  /portfolio      HOLD / RELIST / REDUCE, NAV и ожидаемый GRAM/day
  /gram           GRAM/USDT, волатильность и отставание флоров
  /history ID     покупка, листинги, оценки рынка и итоговый PnL
  /advice 123456  fast/patient exit и объяснение по одной позиции
  /cost 123456 4.25  задать неизвестную себестоимость импортированного гифта
  /exit 123456 3.25  подготовить ручной выход; продажа в минус требует CONFIRM
  /pnl            реализованный и нереализованный, в GRAM и в долларах
  /balance
  /relist 123456  переставить свой лот по текущему стакану

Алерты
  /watch Plush Pepe / Pink Diamond 1200
  /unwatch, /watchlist
  /mute Plush Pepe 4        заглушить коллекцию на 4 часа
  /unmute Plush Pepe

Автобай
  /autobuy            что включено и что мешает купить прямо сейчас
  /arm, /disarm       покупка без участия человека
  /resell on|off      продавать ли купленное самому (по умолчанию off)
  /limits
  /limits set max_ticket 50

Служебное
  /status         поллеры, свежесть данных, прогрев, статистика за сутки
  /auth <данные>  заменить протухшую сессию
```

Разделитель `/` обязателен: и коллекция, и модель содержат пробелы, иначе
`/book Plush Pepe Pink Diamond` не разобрать. Имена можно писать частично и в
любом регистре — если совпадение однозначное, бот его найдёт. Все команды
зарегистрированы через `SetCommands`, так что в Telegram работает меню по `/`.

### Как это ощущается в чате

**Покупка — в два нажатия, не в одно.** Тап по «⚡️ Buy 1240» не тратит деньги: он
меняет клавиатуру на `✅ Confirm` / `✖️ Cancel`, **не трогая текст карточки** —
цифры остаются перед глазами, пока решаешь. Промах пальцем ничего не стоит.

**Карточка перезаписывается результатом.** После покупки сообщение редактируется
на месте, живая кнопка «Buy» исчезает. Ленты из карточек с кнопками, которые уже
нажали, не остаётся.

**Ссылка на лот.** В каждой карточке — «🔗 Open on Tonnel», диплинк
`t.me/tonnel_network_bot/gift?startapp=<id>` прямо в мини-апп. Ты торгуешь
руками, тебе нужно видеть сам лот.

**Кнопки вместо перепечатывания.** `/pos` даёт «♻️ Relist» на каждую позицию —
не надо копировать `/relist 123456` с телефона. `/floor Plush Pepe` даёт кнопки
по моделям, а `/floor` → `/book` → `/hist` переключаются одним тапом в любую
сторону. `/status`, `/pos`, `/pnl` обновляются кнопкой на месте.

**`/auth` удаляет твоё сообщение** сразу после обработки — сессия не остаётся
висеть в истории чата.

---

## Как это работает внутри

Один процесс, живая лента событий, несколько поллеров, SQLite. Ни докера, ни
отдельной СУБД.

**Рынок приходит сам.** У Tonnel есть публичный документированный поток событий
([контракт](https://gifts.coffin.meme/MARKETPLACE_EVENTS.md)) — WebSocket плюс
реплей за семь дней. Каждый новый аск, каждая переоценка, каждая снятая
выставка и каждая закрытая сделка по всему рынку прилетают в ту же секунду, без
авторизации и без расхода лимита запросов. Раньше то же самое добывалось
опросом `pageGifts` раз в две секунды — тридцать одинаковых запросов в минуту с
одного адреса, — и именно этот трафик Cloudflare и резал.

| Источник | Что даёт | Период |
|---|---|---|
| `events` (WS + реплей) | новые аски, переоценки, снятия, **все закрытые сделки рынка** | пуш, мгновенно |
| `stats` — `filterStats` | флор+саплай+редкость **по всему рынку одним запросом** | 60 с |
| `book` — `pageGifts` по модели | лучший конкурирующий аск | по требованию, кэш 15 с |
| `inventory` — `myGifts` + баланс | позиции, undercut, залежавшийся товар | 60 с |
| `scan` — стоящий стакан по моделям | лоты, которые стали интересны без нового листинга | 12 мин |
| `feed` — `pageGifts` sort=latest | **резерв**, работает только когда лента молчит | 2 с |
| `sales` — `saleHistory` | сверка ленты по коллекциям; полный темп, когда ленты нет | 2 мин / 25 с |

Лента не отменяет приватные эндпоинты: она намеренно не отдаёт ни продавца, ни
стакан, ни наш инвентарь. Она отдаёт то, что раньше стоило дороже всего, —
поток. Стакан, снапшот и инвентарь остаются на `gifts2/gifts3`, и теперь им
достаётся весь бюджет запросов.

Одно следствие важно знать: в событиях **нет продавца** — это часть их privacy-
контракта. Поэтому «не оценивать собственный лот» проверяется не по seller, а по
нашей же книге позиций.

Сделка теперь приходит двумя путями — пушем и ротацией `saleHistory`, — и часы у
них разные. Ленту от двойного счёта защищает дедуп по «тот же гифт, та же цена,
±2 минуты»: velocity и оборот считаются по числу строк, и удвоенная сделка
искажает их напрямую.

`filterStats` — основа всего: один запрос возвращает флор и саплай каждой модели
каждой коллекции. Поэтому оценка почти ничего не стоит по трафику, а стакан
дёргается только для тех единиц, что реально попали в кандидаты.

**Скан ходит по площадкам в два прохода.** Portals и MRKT читаются в темпе
человека, листающего мини-апп (MRKT однажды забанил аккаунт за трафик, похожий
на API), поэтому запрос внешней очереди на *каждый лот* был неоплатным: проход
по 40 моделям × 5 асков — это несколько сотен лимитированных запросов, которые
не успевали ни к какому дедлайну. Возвращалось «площадки не ответили» — а это не
косметика, а снятый потолок выхода, тяжёлый штраф скору и жёсткий блок автобая,
причём кэш котировок портился и для фида. Теперь внешняя очередь читается **один
раз на модель** и применяется ко всему её стакану — этого достаточно, чтобы
ранжировать; а верхушка шортлиста перед любым действием переоценивается уже по
своим трейтам («точные трейты → фон → модель»). Нерафинированный кандидат никогда
не двигает деньги и подписан на карточке как «оценка по модели, не по трейтам».

Старая оптимизация «median — потолок выхода» намеренно удалена: протухшая
история не имеет права скрыть живой гэп в стакане или подтверждение Portals.
После локальных проверок кандидат всегда доходит до реального book depth.

### Прогрев

Velocity нельзя посчитать по базе, которая знает только последние пять минут.
При первом запуске бот качает 60 дней истории (`backfill`): последние 14 дней
задают ликвидность, а более длинное окно нужно для разреженных сочетаний
backdrop/symbol. До готовности истории
**не покупает ничего сам**, о чём честно пишет в `/status`.

### Cloudflare

`gifts2.tonnel.network` и `gifts.coffin.meme` отдают 403 обычному Go-клиенту из-за
TLS-фингерпринта. Используется `bogdanfinn/tls-client` с профилем Chrome и одним
стабильным User-Agent на процесс. Популярные Python-обёртки рандомят UA на каждый
запрос — это само по себе аномалия: один TLS-отпечаток с прыгающим UA не выдаёт
ни один реальный браузер.

**Маршруты.** Отказ Cloudflare — это суждение об адресе, а не о запросе.
Поэтому маршрут здесь — самостоятельная сущность: свой TLS-клиент, своя банка
кук (clearance-куки выдаются на адрес — общая банка предъявляла бы чужой
пропуск), своя репутация. `TONNEL_PROXIES` задаёт список **через точку с
запятой**: у резидентских логинов фильтр стран вшит в сам логин
(`…__cr.de,nl,pl,fr`), так что запятая там — данные, а не разделитель; список
через запятую отвергается на старте с этим же объяснением. Собственный адрес
машины всегда в ротации.

**Сначала бесплатное, потом платное.** Прокси считаются продаваемыми по
трафику, поэтому чтения идут с собственного адреса, пока он работает, и
проваливаются на прокси только пока его отказывают. `/status` показывает, сколько
каждый платный маршрут уже потратил и с какой скоростью.

Считается по сокету, а не по телу ответа: Tonnel отдаёт brotli, транспорт
распаковывает прозрачно, и распакованное завышает провод примерно вчетверо
(`filterStats` — 359 КБ после распаковки и 80 КБ на проводе).

**Что сколько стоит на проводе** (сжато, с TLS-оверхедом):

| запрос | байт | как часто | МБ/сутки |
|---|---|---|---|
| `filterStats` — снапшот всего рынка | 80 КБ | 60с | **112** |
| `saleHistory` 50 строк | 3.3 КБ | ротация | 19 |
| `pageGifts` 30 строк (стакан) | 3.8 КБ | по требованию | ~18 |
| `myGifts` + баланс | 5.7 КБ | 60с | 8 |

157 МБ/сутки — это 5 ГБ за месяц ровно, и только если больше ничего не
происходит. Поэтому **пока платим**, каждый поллер держит свой пол по частоте:
снапшот раз в 4 минуты (потолок не вкусовой — автобай отказывается от данных
старше `AUTOBUY_MAX_DATA_AGE` = 5 минут), ротация сделок раз в 10 минут (лента и
так несёт всю ленту сделок), инвентарь раз в 3 минуты, скан раз в 30 минут; кэш
стакана растягивается вчетверо, а глубина режется с 30 асков до 12. Выходит ~40
МБ/сутки — план живёт больше четырёх месяцев. На бесплатном маршруте не
замедляется ничего: свежестью торгуем только когда она стоит денег.

Сам рынок при этом всё равно приходит мгновенно — лента событий бесплатна и идёт
мимо прокси.

Маршрут уходит отдыхать **после трёх отказов подряд**, а не после первого:
ротационный резидентский шлюз выдаёт новый выходной IP на каждое соединение, и
403 там — про адрес, который мы больше никогда не увидим. Отдых нарастает
45с → 3м → 10м → 30м, любой успех обнуляет запись. Сгоревший фиксированный IP
отказывает на каждом запросе, поэтому встаёт почти сразу — ценой двух лишних
попыток. Записи (покупка, листинг) идут по маршруту, который последним ответил,
предпочитая бесплатный: узнать, что адрес испортился, дёшево на странице
листингов и дорого на покупке.

Блоком это считается, только когда отказали **все** маршруты — тогда, и только
тогда, пробуем другой фронтенд и говорим об этом в чат. `floorline smoke` и
`/status` показывают каждый маршрут и кто из них сейчас остывает: вопрос при
блоке всегда «какие адреса ещё живы», и от ответа зависит, ждать или добавлять
прокси.

Три отказа подряд на всех маршрутах → уведомление, автопокупка на паузе, и —
теперь по-настоящему — **поллеры замолкают**. Раньше в этом месте бот писал «поллеры сбавляют темп» и
не сбавлял ничего: тикеры продолжали идти, фид долбил `pageGifts` каждые две
секунды, инвентарь и sales — по своему расписанию, всё это в 403. Блок
Cloudflare — это решение о частоте с нашего адреса; отвечать на него прежней
частотой значит превращать пятиминутный челлендж в многочасовой.

Теперь при блоке приватные эндпоинты не трогаются вообще, а раз в две минуты
уходит **один** пробный `filterStats` — самый дешёвый запрос и самый полезный
ответ. Позволяет себе это только потому, что лента событий продолжает идти:
рынок видно всё это время, недоступна лишь оценка, которой нужен стакан.

Сообщение о блоке приходит не чаще раза в 20 минут, а «отпустило» — только когда
Tonnel действительно ответил *и* отказы прекратились на три минуты. До этого
любой успешный поллер считался восстановлением — включая котировку GRAM, которая
читает публичную биржу и Tonnel не трогает вовсе; она тикает раз в 30 секунд, и
поэтому 14 августа чат несколько часов подряд получал пары ⚠️/✅ каждые полминуты.

`floorline smoke` отличает эти два случая явно: блокировка (403/429) значит, что
сломан транспорт и не заработает ничего; отказ по сессии значит, что транспорт в
порядке и менять надо только authData.

### Подпись `wtf`

Каждый write-запрос требует поле `wtf` — AES-256-CBC от текущего timestamp по
схеме OpenSSL `Salted__` с MD5-KDF. Реализовано на stdlib и проверено тестом
против эталонных векторов OpenSSL: если он падает, покупка и листинг перестанут
работать целиком.

---

### Независимая редкость и «моно»

Всё, что бот знал о гифте, приходило с торговых площадок, а площадка говорит,
сколько за него **просят**, и никогда — что это такое. Каталог `api.changes.tg`
закрывает эту дыру: это сам минт — проценты редкости моделей, фонов и символов,
назначенные при выпуске, и настоящие цвета фонов.

Даёт три вещи:

- **Редкость, которой у Tonnel нет.** Он печатает её суффиксом — «Dark Soul
  (0.4%)» — но далеко не всегда, и бот регулярно оценивал экземпляр, не зная,
  насколько тот редкий.
- **Редкость сочетания.** Три признака независимы при минте, значит их доли
  перемножаются. Флор модели ставит самый дешёвый обычный экземпляр и ничего не
  говорит про тот, которому достались ещё и редкий фон с редким символом.
  Spring Basket · Midas Bunny · Old Gold · Chest — это 1.5% × 1.5% × 0.7%,
  примерно **1 на 635 тысяч**. Неизвестное в произведение не идёт, поэтому ответ
  всегда «не реже чем».
- **«Моно» по цвету, а не по названию.** Раньше совпадение гаммы искалось
  сопоставлением цветовых слов в названиях, из-за чего «Cyberpunk» не был
  фиолетовым, а «Old Gold» с «Midas Bunny» — золотом на золоте. Теперь фон
  классифицируется по своему реальному цвету (`#b58d38` → тёплый), а в проверку
  добавлена модель: раньше сравнивались только фон и символ, и золотая модель на
  золотом фоне не совпадала ни с чем.

Кэш на сутки на коллекцию, один запрос, ходит с адреса машины — к Tonnel
отношения не имеет и платный прокси не тратит.

Автор API просит видимую атрибуцию за бесплатный доступ; строка стоит в `/help`.

### Скан ходит по твоему диапазону, а не по балансу

`/scan 3-5` — показать всё, что стоит от трёх до пяти, независимо от того,
сколько сейчас свободно. Раньше потолок всегда брался из тикета и свободного
баланса, что отвечает на вопрос «что я могу купить прямо сейчас», а спрашивают
обычно другое — «что вообще стоит в этом диапазоне». Открытые концы работают
(`-5`, `3-`), порядок аргументов любой, запятая как разделитель дробной части
тоже.

С явным диапазоном гейт «лот стоит X, а поднять можно максимум Y» из строк
убирается: он про кошелёк, а спросили про рынок. Свободный остаток при этом
печатается один раз сверху, если весь диапазон выше него.

Побочно это ещё и экономит трафик: модели, у которых флор по снимку рынка уже
выше потолка, отсекаются до чтения стакана — а каждый непрочитанный стакан на
платном маршруте это пара килобайт, которые не куплены.

---

## Портфель: чем решается цвет

Раньше цвет позиции определялся тем, выше ли цель входа. Этого мало: Snake Box с
целью +0.2%, шансом уйти за неделю в 1% и двенадцатью продавцами впереди —
формально зелёный, фактически три GRAM, которые ничего не делают. Решает теперь
**отдача на капитал**:

```
productivity = чистыми × P(продать за горизонт) / горизонт(дней) / вложено
```

Горизонт берётся тот, на котором продажа становится вероятнее не-продажи (24ч,
72ч или 7д), а не самый лестный. Ниже 0.5%/день позиция помечается на выход, как
бы красиво ни выглядела цель: те же деньги в следующем сигнале работают лучше.
Внутри одного вердикта список сортируется по этой же отдаче — худшее
использование капитала сверху, потому что весь смысл списка в альтернативной
стоимости.

Ещё два правила, которых не хватало:

- **Не режем аск, если мы и так самые дешёвые на всех площадках.** Цель движка —
  андеркат ближайшего чужого оффера; если этот оффер выше нас, цель оказывается
  ниже цены, которую рынок ещё не отверг, и следование ей отдаёт несколько
  процентов даром.
- **«Переставить на X» пишется только там, где вердикт — переставить.** Строка
  под ДЕРЖИМ заставляла бота спорить с самим собой, а половина этих движений
  стоила две копейки против места в очереди, которое дороже.

Вероятности больше не печатаются как `0%` и `100%`. Пуассоновский хвост по
очереди из горстки офферов их выдаёт охотно, но ни один вход этого не
подтверждает — теперь это `<5%` и `>95%`.

«Залежалась» считается по очереди, а не по обороту модели. Строка «торгуется
1.5 раза в день, должна была уйти за 16ч» печаталась над позицией с двадцатью
офферами впереди: двадцать покупателей при 1.5 в день — это две недели, а не
шестнадцать часов.

---

## Автопокупка

Три барьера подряд. Пройти надо все.

**1. Гейт сигнала** — то, что вообще заслуживает внимания:
risk-adjusted edge ≥ 4.5% · чистыми ≥ 0.25 GRAM · продажа за ≤ 4 дня ·
velocity ≥ 0.5 сделки/день · ≥ 6 сделок за 14 дней · дисперсия ≤ 35% ·
тренд ≥ 0.90 · лот дешевле, чем `max_ticket` и свободный баланс (алерт на 53 GRAM
при тикете 5.5 — это не сигнал, а шум) · нет чужих асков дешевле нашего входа
при нулевой премии за трейты · это не распродажа одного продавца.

Планка эджа **зависит от ликвидности**: при ≥2 дедуплицированных продажах в
день она опускается на 0.5 пункта, при <1 — поднимается на 1.5. Три процента на
модели, которая продаётся 2.5 раза в день, — это позиция, из которой ты выйдешь
завтра; те же три процента на модели, которая продаётся дважды в неделю, — это
две недели замороженного капитала за копейки, и именно в таких сделках десk
застревает.

Сама планка стояла на 1%, пока выход был завышен примерно вдвое — то есть
пропускала всё, что не было прямым убытком. Абсолютный порог рядом с процентным
нужен потому, что 5% от трёхгрэмового лота — это семь сотых GRAM: процент есть,
денег нет. А «распродажа» — это когда три и больше лотов одной коллекции уже
прошли по той же цене за час: дефицита нет, и покупатель возьмёт следующий за те
же деньги.

**2. Гейт автопокупки** — «это точно легко продать»:
risk-adjusted edge ≥ 6% · velocity ≥ 1/день · ≥ 10 сделок · turnover разных физических gifts
≥ 0.60 (доступная защита от накрутки: API не отдаёт продавцов) · дисперсия ≤ 25% ·
тренд ≥ 0.95 · ожидаемое время выхода ≤ `max_exit_days` ·
прогрев завершён · движение GRAM за 15 минут не больше 3%.

### Атрибуты, score и портфель

Для гифта считаются четыре разные цены: `liquidation`, `fast_exit`, `fair_value`
и `patient_ask`; BUY всегда считается только от `fast_exit`, и это единственная
из них, которую не считает микс — см. выше. `fair_value` собирается из 45%
живого стакана, 30% свежих продаж, 20% внешней глубины и 5% трейтов. Вес истории
зависит от числа уникальных физических гифтов: 10% до 5, 20% при 5–9, 30% при
10–19 и 40% после 20; при малой выборке сама history-price дополнительно
сжимается к live depth. Повторные продажи одного NFT учитываются один раз.
`patient_ask` никогда не ниже `fast_exit`, а падающий сценарий показывается
отдельным `bear case`.

Глубина считается только по непрерывной части стакана. Скачок больше 20% от
предыдущего аска обрывает «живую» глубину, а сама depth-опора не может стоять
выше первого чужого аска больше чем на те же 20%: стакан `4.21 / 7.90 / 10.20`
— это одна реальная цена и две хотелки. Дырявый стакан помечается на карточке и
закрыт для автобая. `fair_value` и `patient_ask` дополнительно не могут пробить
третью ступень внешней очереди — без этого дырка в локальном стакане печатала
«фэйр 10.9» там, где Portals стоял на 3.77.

Если стакан и лента расходятся больше чем на 35%, цена выхода сжимается в
полосу, которую поддерживает история, и помечается как **интерполяция**: это не
факт, а середина между двумя источниками, которые спорят. Скор такую сделку
почти обнуляет — иначе выдуманная цена ранжируется как наблюдённая.

Премии атрибутов берутся только из реальных сделок и при 2–3 точных продажах
равны нулю: бот пишет, что данных мало. Редкость сама по себе цену не добавляет.
Отдельно оценивается **внешка** — тёмные/ониксовые фоны, совпадение гаммы фона и
символа, красивые номера. Она никогда не поднимает цену: флор к ней слеп, но
доказательством служат только сделки. Что она решает — можно ли вообще
применять к этому гифту чужой стакан. Кроссмаркет ищет сравнимые по лестнице
«точные трейты → фон → модель»; если по фону сравнить не с чем, бот оставляет
консервативное число, но пишет, что считал по обычным экземплярам, и снимает
такой лот с автобая.

### Вердикт — это самый слабый слой, а не самый сильный

`✅ BUY · скор 3/100 · данные 3%` — это не проблема вёрстки, это сломанное
предложение. BUY читается как «модель уверена», поэтому BUY на трёх процентах
доверия — это интерфейс, утверждающий то, во что движок не верит. Гейты, которые
его выдавали, все были про **цену**, а цена — только одна из трёх вещей, которые
должны быть верны.

```
цена         есть ли деньги после комиссий и риск-буфера
ликвидность  можно ли это реально продать и в какой срок
данные       знаем ли мы достаточно, чтобы первые два что-то значили
```

Вердикт — минимум из трёх. Не прошла цена → `PASS`. Прошла цена, но не
ликвидность → `РУКАМИ`. Прошли цена и ликвидность, но данных мало → `SPEC`.
`BUY` бывает только когда держатся все три, и автобай работает только на нём.
Карточка называет слой, который тянет вердикт вниз, — раньше она показывала
пониженный вердикт и оставляла угадывать, какое из двадцати чисел его уронило.

### Вероятность продажи вместо выдуманных «~1.6д»

Одна цифра дней с точностью до десятой была фикцией дважды. Она получалась
делением очереди на среднюю за 14 дней скорость — это среднее, а не расписание, —
а очередью считался **весь стоящий саплай модели**. Сорок выставленных гифтов, из
которых под нашей ценой два, — это очередь глубины два, остальные тридцать восемь
неважны, пока мы не просим дороже них.

Теперь это кривая выживания. Покупатели приходят потоком, каждый берёт самый
дешёвый оффер на любом экране, нас покупают, когда пришло достаточно покупателей,
чтобы разобрать всех, кто дешевле. Это пуассоновский процесс, и он даёт три
числа, которые стоит печатать:

```
продать: 24ч 38% · 72ч 74% · 7д 92%
```

Важная поправка: выставившись под всех, мы первые в очереди **в этот момент** по
построению. Модель, читающая только текущую очередь, объявила бы любой быстрый
выход одинаково быстрым. Поэтому продавцы, стоящие вплотную сверху, считаются
теми, кто заберёт это место обратно, — примерно половина из них за многодневный
горизонт. Это единственное число здесь, не измеренное по данным, и первое, что
надо откалибровать по журналу наблюдений.

Та же машинка отвечает и на обратный вопрос: не «один быстрый выход», а
**исполнимая цена под срок** — сколько можно просить и всё ещё рассчитывать уйти
за 24ч / 72ч / 7д. Именно по ней судится покупка, и это не fair value. Гифт может
стоить 8, иметь 72-часовой выход 5.1 при входе 4.9 — и тогда восьмёрка это факт о
чужой сделке, не о нашей.

### Чужие площадки: фильтр бреда

Самый большой источник выдуманной стоимости. Vice Cream за 3.16 получал «опора 15
· +375% к входу» с очереди Portals по фону; Pool Float за 3.82 получал 159.1 —
середину между реальным лотом 12.2 и фантазией 306. Обе цифры потом несли пятую
часть веса в price discovery, а раздутый fair value уходил в цель портфеля,
которую невозможно исполнить.

Три правила. **Один аск — не цена**, а число, которое кто-то набрал: венью с
единственным листингом даёт факт об исполнении и ноль информации о стоимости.
**Два аска говорят, только если согласны между собой**, и тогда говорят более
дешёвым — усреднение 12.2 и 306 и дало те самые 159.1. **Три и больше** —
медиана трёх дешёвых, устойчивая в обе стороны: и к наживке снизу, и к фантазии
сверху.

Поверх этого — санитарная полоса относительно исполнимого рынка (живой чужой аск,
иначе история). Опора вне `[0.4×, 2.5×]` выбрасывается целиком и не возвращается
даже как «расхождение»: площадки с нами не спорят, просто одна из них описывает
не этот гифт. Полоса намеренно широкая — площадки честно расходятся, и фильтр,
срабатывающий на обычном расхождении, убил бы всю пользу кроссмаркета. Отдельно
режутся аски **снизу**: фантомный дешёвый аск становится walkaway и придавливает
выход к цене, которой не существует, — так здоровая позиция получает вердикт
«СОКРАТИТЬ/ВЫЙТИ».

### Режим рынка

Падающий рынок — это не тот же рынок с числами похуже. Раньше одна логика
применялась везде, и поэтому модель, у которой принты были хорошие неделю назад и плохие с тех пор, продолжала читаться как возможность: медиана за 14 дней, с
которой её сравнивали, всё ещё содержала хорошую неделю.

Три режима по последней неделе против всего окна меняют три вещи: сколько эджа
требовать (+2 пункта на падающем), сколько держать (лимит × 0.6) и насколько
верить старой половине истории (опора сдвигается на 70% к недельной медиане).

### Adverse selection

Лот на −15% к флору бывает подарком, а бывает началом сползания. Отличается это
не ценой, а тем, что происходило вокруг: если за 15 минут в модели появилось
2–3 новых лота дешевле нашего, а лента идёт вниз — мы покупаем **в** движение, а
не против него. Обе половины обязательны: свежий саплай сам по себе это активная
модель, падающая лента сама по себе это дешёвая модель.

Score ранжирует сделки по risk-adjusted ROI в день: из честного net-edge
вычитается буфер неопределённости, затем результат делится на время выхода и
умножается на качество доказательств и на размер сделки относительно
`max_ticket`. Поэтому быстрые 2–5% могут стоять выше медленных 10%, а +0.15 GRAM
на трёхгрэмовом лоте больше не конкурирует с +5 на пятидесятиграммовом. Гэп над
первым аском не даёт **ничего**: продаём мы под первым аском, а не в гэп — за
эту награду движок когда-то выдал свой рекордный скор 136.5 на стакане
`8 → 14.4` с одной живой ценой. Размерный множитель применяется только при
ранжировании покупок: у позиции, которая уже куплена, капитал вложен, и малый
лот не становится от этого худшим — раньше он молча сортировал портфель по
размеру лота.

Очередь **на других площадках** тоже входит в скор. Быстрый выход быстрый ровно
потому, что подрезает самый дешёвый оффер на любом экране; сколько это стоит,
решает то, что стоит сразу за ним. Один оффер в 5% над нами — это очередь, в
которой мы первые; пять — это ценовой уровень, и первый заметивший забирает
позицию обратно, причём на площадке, где мы даже не можем переставиться. Раньше
эту половину картины не смотрел никто: как только внешняя очередь становилась
потолком выхода, depth-фактор по построению отходил в сторону (дырка в локальном
стакане больше ничего не держит), а cross-фактор считал только расхождение
опор — то есть очередь, через которую реально предстоит продавать, оставалась
неизученной ровно в тех случаях, где она и определяла цену. Эти же офферы входят
в буфер неопределённости: каждый сосед по цене — это тот, кто может забрать
сделку, сдвинувшись на сотую.

Исторические сделки приводятся к текущему курсу GRAM/USDT с жёстким лимитом
коррекции ±5%, а `/gram` показывает модели, чей floor не успел перестроиться за
курсом. Portals/MRKT читаются как лестница до 20 активных листингов; межрыночный
reference — медиана первых трёх асков, поэтому один случайный floor не управляет
price discovery, но **потолок выхода** задаёт именно самый дешёвый оффер.
Расхождение history и live depth свыше 10% либо площадок свыше 15% переводит
сделку в режим «только руками». Ответ площадки кэшируется на 5 минут, но провал
по **нашему собственному** дедлайну — нет: то, что у нас кончился бюджет
времени, ничего не говорит о площадке, а запомненное как ответ оно на все пять
минут снимало потолок выхода, роняло скор и блокировало автобай на каждой
карточке подряд.

Shadow-режим и требование калибровки живут в БД и переключаются в боте —
`/autobuy` показывает чеклист всех барьеров с кнопкой снять каждый.
`SHADOW_MODE` в `.env` задаёт только начальное значение при первом запуске.

Планка эджа зависит от того, как быстро можно выйти: при ≥2 дедуплицированных
продажах в день она опускается на 0.5 пункта, при <1 — поднимается на 1.5. Три
процента на модели, которая продаётся 2.5 раза в день, — позиция, из которой ты
выйдешь завтра; те же три процента на модели, которая продаётся дважды в
неделю, — две недели замороженного капитала за копейки.

Инвентарь читается со всех страниц. Себестоимость внешних покупок восстанавливается
по истории конкретного gift; если это невозможно, `/cost` запрашивает её вручную.
Для каждой позиции ведётся неизменяемый lifecycle: обнаружение ручной или
автоматической покупки, источник и confidence себестоимости, выставление,
каждый ручной/автоматический репрайс, снятие с продажи, изменения рекомендации и
подтверждённая продажа. Раз в 10 минут сохраняется mark-to-market с текущим ask,
исполняемым выходом, внешним ask-depth reference, GRAM/USD, edge, временем выхода
и score. Исчезновение из inventory не считается продажей без совпавшей сделки:
позиция получает статус `missing` и продолжает проверяться, поэтому transfer или
временный сбой API не создаёт выдуманный PnL. Позиции оцениваются по всем площадкам сразу: цель релиста прижата к самому
дешёвому чужому офферу где угодно, а `/pos` и `/advice` прямо считают, сколько
офферов стоит дешевле нашего аска и на какой площадке ближайший. Порядок в
`/pos` — по срочности (что требует решения, и среди этого — где больше денег),
а не по скору: скор для позиции равен нулю всюду, где быстрый выход ниже входа,
то есть почти везде, и книга сортировалась фактически случайно. Весь журнал
доступен через `/history gift_id`. Закрытые buy→sell циклы хранятся отдельно и не теряются,
даже если тот же физический gift ID позже куплен повторно.
Автоматический репрайс допускается только при явном вердикте `ПЕРЕСТАВИТЬ`, без
спора рынков, не чаще раза в 6 часов, не более чем на 15% за шаг и никогда ниже
`entry + min_markup`. Убыточный выход остаётся только ручной рекомендацией.

**3. Риск-лимиты** — сколько денег вообще можно потратить:

```
/limits set max_ticket 50
/limits set daily_budget 300
/arm
```

`max_ticket` и `daily_budget` **обязательны** — без них `/arm` откажет. Дневной
бюджет лежит в БД, поэтому рестарт не вернёт уже потраченное.

Прочие лимиты: `max_positions`, `max_buys_per_hour`, `model_cooldown_min`,
`min_balance_reserve`, `min_markup`, `max_exit_days`.

**Авто-разоружение** при: трёх неудачных покупках подряд · 403 от write-хоста ·
отклонённой сессии Tonnel.

**Идемпотентность.** Заявка на покупку резервируется в БД *до* отправки запроса,
поэтому двойной тап по кнопке, ретрай или падение посреди операции не превратятся
в две покупки. При обрыве связи бот не повторяет запрос, а спрашивает у маркета,
чем мы владеем.

**Автолистинг.** Сразу после покупки стакан перечитывается и лот выставляется по
пересчитанной цене выхода. Если рынок за это время ушёл и продажа означала бы
цену ниже `вход + min_markup` — бот **не выставляет** и пишет об этом. Фиксировать
убыток за тебя он не будет.

---

## Что ещё детектится

- **Undercut** — твой аск перестал быть лучшим **на любой площадке**, не только
  на Tonnel. Быть самым дешёвым лотом на одной площадке — это не быть самым
  дешёвым оффером на экране; раньше единственный сторож, чья работа ровно в этом,
  смотрел в один стакан, и позиция могла неделю стоять за очередью Portals,
  числясь держателем флора.
- **Залежавшаяся позиция** — висит втрое дольше, чем velocity модели предсказывает.
- **Sweep** — всплеск сделок по модели от нескольких разных покупателей.
- **Обвал флора** — по вотчлисту и позициям, падение >15% за час.
- **Новая коллекция** — незнакомое имя в `filterStats`. Первые часы дропа —
  самое волатильное окно на этом рынке.

## Встроенный бэктест

Каждый сигнал пишется в таблицу `signals` вместе с ценой, выходом и edge. Сделки
копятся в `sales`. Через пару недель хватит одного SQL-джойна, чтобы посчитать,
какая доля сигналов реально продалась выше входа и за сколько дней — без единой
строчки дополнительного кода. **Не поднимай пороги и не арми автопокупку, пока не
посмотришь на эти цифры.**

---

## Разработка

```
make test     # go vet + go test ./...
make build
make smoke    # требует живой .env
```

Структура:

```
cmd/floorline      CLI: run | smoke | backfill
internal/tonnel    приватное API: клиент, подпись wtf, фильтры, типы;
                   плюс лента событий (stream.go, events.go) — публичная и
                   документированная, живёт в том же пакете, потому что
                   отвечает про тот же рынок теми же сущностями
internal/store     SQLite: листинги, сделки, снапшоты, позиции, сигналы, риск
internal/pricing   ликвидность, стакан, оценка (здесь живёт триггер)
internal/signal    гейты сигнала и автопокупки, скоринг, дедуп
internal/risk      лимиты, kill switch, дневной бюджет
internal/exec      покупка + автолистинг + идемпотентность
internal/app       поллеры, детекторы, рендер карточек
internal/bot       Telegram
internal/market    read-only сравнение цен: Portals + MRKT за общим интерфейсом
```

## Оговорки

Эндпоинты Tonnel приватные и недокументированные — они могут измениться без
предупреждения. Всё изолировано в `internal/tonnel`; `floorline smoke` за
несколько секунд скажет, что именно сломалось. Лента событий — исключение: она
публичная и с описанным контрактом, поэтому неизвестные типы событий и новые поля
игнорируются, а не ломают разбор, как контракт и просит.

Автоматизация может противоречить правилам площадки. Дефолтные частоты поллинга
подобраны так, чтобы нагрузка выглядела человеческой; поднимать `READ_RPS` —
быстрейший способ словить бан.

`.env` и `*.db` в `.gitignore`. Токен и authData не пишутся в логи.
